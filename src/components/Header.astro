<header class="header">
  <div class="menu-icon">
    <i class="fas fa-bars"></i>
    <span class="menu-text">menu</span>
  </div>
  <i class="fas fa-shopping-bag cart-icon"></i>
</header>

<style>
  /* General Reset for Consistency */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  /* Header Styling */
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%; /* Prevent horizontal scrolling */
    max-width: 100vw; /* Ensure it doesn't exceed the viewport width */
    padding: 1rem 2rem;
    position: fixed; /* Stays at the top on scroll */
    top: 0;
    left: 0;
    z-index: 100; /* Ensures it stays on top of other content */
    background-color: white; /* Adjust as needed */
  }

  /* Menu Icon and Text */
  .menu-icon {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .menu-text {
    font-family: "Kantumruy Pro", sans-serif; /* Adjust font to match project */
    font-size: 1rem;
    cursor: pointer;
  }

  /* Cart Icon */
  .cart-icon {
    font-size: 1.5rem;
    cursor: pointer;
  }

  /* Responsive Adjustments */
  @media (max-width: 768px) {
    .header {
      padding: 0.5rem 1rem;
    }

    .menu-text {
      font-size: 0.875rem; /* Slightly smaller text for mobile */
    }

    .cart-icon {
      font-size: 1.25rem;
    }
  }
</style>

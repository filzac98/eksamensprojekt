<header class="header">
  <div class="menu-icon">
    <div class="burger-menu">
      <div class="burger-line top-line"></div>
      <div class="burger-line middle-line"></div>
      <div class="burger-line bottom-line"></div>
    </div>
    <span class="menu-text">menu</span>
  </div>
  <i class="fas fa-shopping-bag cart-icon"></i>
</header>

<style>
  /* General Reset for Consistency */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  /* Header Styling */
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 100vw;
    padding: 1rem 2rem;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    background-color: white;
  }

  /* Burger Menu Container */
  .menu-icon {
    display: flex;
    align-items: center; /* Align items (burger + text) horizontally */
    gap: 1rem; /* Space between the burger and "menu" text */
    cursor: pointer;
  }

  /* Burger Menu */
  .burger-menu {
    display: flex;
    flex-direction: column; /* Stack the lines vertically */
    gap: 6px; /* Space between the lines */
  }

  /* Burger Lines */
  .burger-line {
    height: 4px; /* Thickness of the lines */
    background-color: black; /* Line color */
    border-radius: 2px; /* Rounded edges */
  }

  /* Top Line */
  .top-line {
    width: 27px; /* Shorter top line */
    align-self: flex-start; /* Align to the left */
  }

  /* Middle Line */
  .middle-line {
    width: 40px; /* Full-length middle line */
    align-self: flex-end; /* Align to the right */
  }

  /* Bottom Line */
  .bottom-line {
    width: 27px; /* Shorter bottom line */
    align-self: flex-end; /* Align to the right */
  }

  /* Menu Text */
  .menu-text {
    font-family: Arial, Helvetica, sans-serif;
    font-size: 1rem;
  }

  /* Cart Icon */
  .cart-icon {
    font-size: 1.5rem;
    cursor: pointer;
  }

  /* Responsive Adjustments */
  @media (max-width: 768px) {
    .header {
      padding: 0.5rem 1rem;
    }

    .menu-text {
      font-size: 0.875rem;
    }

    .cart-icon {
      font-size: 1.25rem;
    }

    .burger-line {
      height: 3px; /* Thinner lines for smaller screens */
    }
  }
</style>
